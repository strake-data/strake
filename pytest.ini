[pytest]
testpaths = tests strake-python/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    benchmark: tests that are slow and require specific data setup (skipped by default)
    timeout: marks tests to be timed out after a specific duration
addopts = -v --tb=short -m "not benchmark"
